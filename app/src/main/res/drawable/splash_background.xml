<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android"
    android:opacity="opaque"> <!-- android:opacity="opaque" should be here -->
    <item>
        <!--this is your background, you can use color, gradient etc.-->
        <color android:color="@color/tech_arion"/>
    </item>
    <item>
        <bitmap android:src="@drawable/logo_white"
            android:gravity="top"/>
    </item>
    <item android:width="50dp"
        android:height="50dp"/>
</layer-list>